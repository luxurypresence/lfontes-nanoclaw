# Terminal Agent

You are Terminal Agent.

## Shared knowledge (lfontes-mono)

`$LFONTES_MONO_ROOT` points to the shared wiki and skills (mounted RW). Start with `$LFONTES_MONO_ROOT/wiki/index.md` for the content catalog; the rest is discoverable.

Writes leave the working tree dirty — Luis reviews and commits manually.
